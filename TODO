Todo:
add readme
	BUNGEECORD
	RESOURCES
		CONFIGS
		JARS
		SPIGOT
		TMP
	SCRIPTS
		UPDATE
			instead of looking at a changed file just compare md5's of every file? if file doesnt have one to compare to, copy it over!
		run spigot update in the background.. it takes a while..
		finish logger script
		create a system to add servers to bungeecord automagicly
		
	SERVERS
	
Testing, PaperSpigot Testing, Logging re-do
